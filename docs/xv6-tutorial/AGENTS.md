## Tutorial context

Before changing the tutorial curriculum, manifest, learning units, experiments,
validation tooling, or release status, read `CONTEXT.md` and the relevant
accepted decisions under `adr/`.

This subtree keeps its glossary and decisions local so the standalone tutorial
can evolve independently. See `adr/0023-local-tutorial-domain-discovery.md` for
the scoped exception to the repository's default domain-doc layout.
